/**
 * UI Components Feature
 * 
 * Provides shared UI components that don't belong to specific feature domains.
 * Currently empty but reserved for future shared UI components.
 */

// Currently no components - this feature is reserved for future shared UI components
// that don't belong to specific domains like theme, data-management, or i18n.