// BubbleMenu is deprecated: the main editor now renders an inline selection toolbar directly.
// Keep a lightweight stub to avoid breaking imports in other places. Use the selection toolbar
// built into `tiptapTextEditor.tsx` instead.
export default function BubbleMenu() {
  return null;
}
